version: 1
deploy:
  steps:
    setupVariables:
      after:
        - echo "credentials \"app.terraform.io\" { \n  token = \"${TF_CLOUD_TOKEN}\"\n}" > .terraformrc
destroy:
  steps:
    setupVariables:
      after:
        - echo "credentials \"app.terraform.io\" { \n  token = \"${TF_CLOUD_TOKEN}\"\n}" > .terraformrc
  onCompletion:
    - echo "destroy-onCompletion!!!"
