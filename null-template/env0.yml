version: 1
deploy:
    steps:
        setupVariables:
          after:
            - pwsh -v
            - aws --version
            - gcloud --version
            - az --version
            - python --version
            - python3 --version
            - git --version
            - npm --version
            - yarn --version
            - node --version
            - pip --version
            - pip2 --version
            - pip3 --version
            - kubectl version --client
            - tfenv --version
            - terraform --version
            - echo "wow2"
destroy:
    onCompletion:
      - echo "destroy-onCompletion!!!"
