version: 1
deploy:
    steps:
        setupVariables:
          after:
            - aws --version
            - gcloud --version
            - az --version
            - git --version
            - ./test.sh

destroy:
    onCompletion:
      - echo "destroy-onCompletion"
