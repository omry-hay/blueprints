version: 1
deploy:
  steps:
    setupVariables:
      after:
        - printf "%s" "$(<env0.auto.tfvars.json)"
        - printf "%s" "$(<env0.env-vars.json)"
        - printf "%s" "$(<env0.configurations.json)"
        - printf "%s" "$(<env0.system-env-vars.json)"
